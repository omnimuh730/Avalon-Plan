# Relay server (browser extension pairing)
VITE_AVALON_SERVER=http://localhost:3847

# AI BFF — API keys are configured in packages/ai-bff/.env
VITE_AI_BFF_URL=http://localhost:3920
# Optional: override model (otherwise uses ai-bff DEFAULT_MODEL, e.g. deepseek-v4-flash)
# VITE_AI_MODEL=deepseek-v4-flash
